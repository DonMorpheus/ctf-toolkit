# Cohort (HTB)

- `writeup.md` — short path (no flags)
- `scripts/marimo_terminal_rce.py` — CVE-2026-39987 helper

```bash
python3 scripts/marimo_terminal_rce.py --url 'wss://TARGET/terminal/ws' -c id
```
